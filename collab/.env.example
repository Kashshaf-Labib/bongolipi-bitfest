# Port the realtime server listens on
PORT=1234

# Shared secret used to verify collab tokens minted by the Next.js app.
# Must match COLLAB_SECRET in the client app.
COLLAB_SECRET=

# MongoDB connection (same cluster as the app) for persisting Yjs state.
MONGODB_URI=
DBNAME=bongolipi
